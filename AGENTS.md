# Prototype Instructions

Run the local server yourself and open the preview in the in-app browser. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Preview Device

Driver端 H5 原型预览采用 **iPhone 17** 逻辑分辨率：**402 × 874 CSS px**（`.phone-frame` 外框含 10px 边框后为 422 × 894）。

## 长期移动端视觉参考

- 用户指定 Dribbble 的「[Popular · Mobile · App](https://dribbble.com/search/shots/popular/mobile?q=app)」作为移动端设计灵感库。后续设计可借鉴其中成熟的层级、排版、卡片密度、色彩节奏与动效反馈，但不得直接复刻单一作品的界面或品牌资产。
- 用户指定 Dribbble 的「[Web](https://dribbble.com/search/web)」作为后台网页设计参考。适合吸收首屏层级、数据面板、桌面端栅格、留白、叙事性图形和页面过渡；后台仍需遵循既定“企业信任”设计系统与可维护的组件化实现。
- 用户指定 [Awwwards UI Design](https://www.awwwards.com/websites/ui-design/) 作为高标准网页体验参考。用于学习精确的版式、视觉节奏、交互反馈和整体完成度；展示型 3D、滚动叙事与高成本动画必须审慎筛选，不能干扰业务效率、加载性能或无障碍访问。
- 用户指定站酷的「[UI 精选](https://www.zcool.com.cn/discover/17?page=1)」作为中文设计语境的补充参考库。它尤其用于吸收产品叙事、品牌调性、中文字排版和完整视觉方案；实施时仍需回归当前产品的任务流、可访问性与真实数据密度。
- 对车队调度类页面，优先将灵感转化为可用性：先保证地图、车辆状态、告警与主操作的扫读效率，再采用清晰的大数字指标、有限而语义化的强调色、紧凑卡片分组与稳定的底部导航等视觉手法；不为装饰牺牲高频操作与状态辨识。

## Durable product constraints

- 后台保留站点管理与圈统计报表；站点管理页面结构、字段和示例数据保持原样。
- 司机端继续保留；电子围栏、地址池、识别规则等配置页面和代码均移除。圈统计报表只展示统计结果，不展示识别规则配置。
- 电子围栏的新增与编辑表单不展示“所属片区”，既有围栏—片区关联仅在片区管理中维护，编辑围栏不得改写该关联。
- 环线趟次按 PRD 采用“圈起点地址池 → 圈终点地址池”的车辆状态机识别，不维护片区—围栏二级关系，也不配置固定中间线路。
- 后台页面以 `/Users/dmm/Desktop/环线/` 为只读源；当前项目通过 `npm run sync:admin -- /Users/dmm/Desktop/环线` 同步后台副本，禁止修改源目录。
- 当前后台的“站点管理”是本地扩展页，独立于电子围栏；其启用状态、名称、坐标、电价、充电桩及停留时长通过 `/api/stations` 直接控制司机端场站地图与列表。
- Netlify 部署必须同时提供可用的后台 API：`/api/*` 由 Netlify Function 承载，围栏、场站、趟次、运费与片区数据写入站点级 Netlify Blobs，跨部署保持；本地开发继续使用 `admin-server` 的文件存储。
- 车队长端场站地图与列表仅复用司机端既有场站能力，不增加额外业务功能；场站列表默认展示“不限”距离分类，仅保留名称搜索与距离筛选，不提供省市筛选。
- 车队长端不提供任何下载、导出及下载任务功能；日报表和明细数据仅供在线查看。
- 车队长监控地图展示当前用户的实时定位点：优先使用设备方向数据，缺失时由连续定位坐标推算朝向，并以方向箭头渲染。
- 关闭地图上的充电场站详情卡后应直接返回纯地图，不自动恢复或弹出此前的车辆信息窗；车辆卡仅由用户点击车辆点位或车辆清单后打开。
- 车队长端的最终交付形态为微信小程序，采用 `uni-app`，而非 H5；现有 `/#/fleet` 仅作为交互原型入口。后续车辆监控需按微信小程序的地图、缓存、鉴权和订阅消息能力设计，不将 H5 方案直接作为生产实现。
- 车队长小程序不采用后台的“企业信任”视觉风格。其 UI 应按移动端一线车队调度场景独立设计，优先保障地图可读性、状态识别效率、单手操作与高频异常处置；后台继续沿用“企业信任”风格。
- 车队长端范围已于 2026-07-31 扩展为《小程序车辆监控功能需求文档》全量（P0→P3），不再是原 MVP 子集。已交付：首页概览与六宫格入口、监控地图（全部/在线/离线/从未上线）、车辆信息窗、车辆列表分页、三 Tab 车辆状态卡、7 类单车实时数据、轨迹回放（分段+进度+联动数据）、行车/充电日志及行程轨迹与充电位置、SOC 过低预警（15%/30% 阈值）、单车日报表与明细数据。
- 车队长端代码位于 `src/fleet/`（`data.js` 数据、`maps.jsx` 地图、`components.jsx` 共用件、`pages/` 各页），`src/App.jsx` 仅通过 `FleetLeaderApp` 引用。修改车队长端时只动这个目录，不要回写 App.jsx。
- 车队长演示数据为 34 辆车，覆盖在线 24 / 离线 6 / 从未上线 4，SOC 阈值 15% 与 30% 两档均有低电量样本；数据用确定性伪随机生成，刷新结果稳定，便于视觉回归比对。首辆演示车额外保留 6 段行程，用于验证历史轨迹横向滑动卡片。
- 车队长监控页以 `/Users/dmm/.codex/generated_images/019f700c-0a9c-7cf2-b900-b41881d0519e/exec-75be81c1-439f-4eb1-b3ce-a0b7cfd43847.png` 为主要视觉基准：不显示顶部车牌下拉、日期筛选、告警或轨迹分享操作条；保留左侧完整车辆状态筛选抽屉和展开式车辆详情面板；底部导航固定为“首页 / 监控 / 我的”，默认选中监控，三个导航项必须无间距平铺。
- 车队长端底部导航采用三入口：“首页 / 监控 / 我的”。首页融合经营统计、车辆监控分析、业务报表、预警与调度工作台；原调度页的全部 15 项业务入口迁入首页“经营调度”维度，不展示今日待办卡片。监控页保留地图与车辆状态处置。
- 车队长端不提供车辆收藏或关注能力：车辆详情不展示关注按钮，“我的”不展示关注车辆列表或基于关注关系的监控车辆入口。
- 首页不展示右上角“实时监控”按钮；标题下明确展示当前登录人的组织数据口径，演示默认路径为“云南力伏星新能源有限公司 / 云南钦圣新能源科技有限公司 / 一车队（云南钦圣）”。首页提供“运输经营分析 / 车辆运行分析”两个可切换维度；生产版按登录态组织、部门、车队 ID 过滤所有统计、车辆与预警数据。
- 首页组织范围弹层允许按“组织 → 部门 → 车队”逐级切换，切换后车队长端的首页、监控地图、车辆列表与预警均按当前范围过滤。车辆运行分析避免重复低电量信息：SOC 低电量仅在预警列表呈现，运行面板优先展示在线率、平均 SOC、行驶中、充电中、里程和充电运行等互补指标。
- 车辆运行分析顶部状态栏固定为“在管车辆 / 在线 / 离线 / 行驶中 / 充电中”；离线为当前非在线车辆总数，连续离线满 24 小时的数量以橙色“异常”标识。
- 车辆运行分析五项状态指标下钻共用一套车辆列表模板，仅通过页面标题和固定状态筛选控制；列表页不提供状态分类切换，从哪个指标进入即只展示该指标对应的数据。离线下钻仅筛选当前离线车辆，异常离线（≥24 小时）置顶并展示橙色标识与时长；全部车辆默认在线优先。列表仅支持车牌/VIN搜索、每次 20 条的上拉续载和统一车辆详情跳转，不提供导出入口。
- 车队长端所有二级列表页的分类/筛选栏必须完整位于顶部导航栏下方；内容滚动时筛选栏保持可见，不得被顶部裁切或遮挡。
- 从监控车辆信息卡进入任意二级页时，顶部必须固定显示清晰可点击的返回按钮；点击后回到直接来源页并保留此前状态。
- 监控车辆信息卡进入的“单车监控 / 历史轨迹 / 任务单”二级页，页头统一复用场站列表的样式与标题层级：单行页面标题、左侧返回图标、与内容区相同的顶部留白；不在页头额外展示车辆副标题或“返回”文字。
- 车辆运行分析第二行指标包含“今日行驶”和“今日充电”；今日充电按当日零点至当前时刻、当前组织范围内全部车辆的充电日志电量累计，以 kWh 展示。
- 首页“今日行驶”直接复用行车日志页：固定为今日范围，不展示近 7 天/近 30 天/自定义、车辆与记录数提示、标题下车牌或车辆下拉筛选；默认查看全部车辆，仅支持页面内车牌/VIN搜索，轨迹查看仍复用现有单次行程轨迹页。
- 今日行车日志页在搜索框上方固定展示与首页同源的一行汇总：总里程、总行程次数、涉及车辆数，用于核对首页“今日行驶”指标。
- 今日行车日志卡片以“左侧突出车牌、右侧高亮里程”为第一信息层；第二行弱化起止位置；详情区分别展示完整的开始时间、结束时间与 SOC 消耗，右侧保留“查看轨迹”。所有行车日志起止时间统一采用“YYYY-MM-DD HH:mm:ss”，行车日志及单次轨迹中的百分比能耗字段统一命名为“SOC消耗”。
- 今日行车日志仅展示当前组织范围内且能关联到有效车辆档案、具有车牌号的记录，不展示跨范围或无车牌示例数据。
- 首页“今日充电”指标进入车队级今日充电日志页，复用今日行车日志的汇总、搜索、卡片和空状态结构，但字段保持充电业务口径：总充电量、充电次数、涉及车辆、充电量、场站、起止 SOC、充电时长与位置查看；快捷入口“充电记录”仍进入当前车辆的历史充电日志。
- 今日充电日志卡片车牌必须通过充电记录 VIN 关联车辆档案；位置展示由充电经纬度逆地理编码得到的街道 / 路段级地址，失败时统一显示“位置信息暂无”，不得以场站名代替；整张卡片与“位置查看”按钮均打开同一充电位置地图点位。
- 所有充电日志的开始时间与结束时间统一采用“YYYY-MM-DD HH:mm:ss”；今日充电卡片分别完整展示开始时间、结束时间、起止 SOC 与充电时长，不再仅展示结束时分。
- 车辆运行分析下方四个快捷入口固定顺序为“车辆列表 / 单车日报 / 充电记录 / 行驶记录”；保留图标、统一高度与对应跳转。
- 车辆运行分析下方通用“车辆列表”入口提供双行组合筛选：第一行为全部 / 在线 / 离线 / 行驶中 / 充电中五个 TSP 状态，第二行为全部 / 有任务 / 待运输 / 无任务 / 停运五个 TMS 任务状态；五个运行指标下钻页仍保持固定入参，不展示该双行筛选。
- 通用车辆列表卡片核心层固定三层：车牌与 TSP/TMS 双标签、车型与 VIN、SOC/总里程/当前位置；业务扩展层继续展示挂车车牌与当前司机两个独立信息块。TMS“无任务”需显示灰色状态标签，仅接口未返回任务状态时隐藏标签。
- 车辆运行分析的四个快捷入口卡片保持统一高度，不能因图文长度造成上下两行高度不一致。
- 车辆运行分析底部口径说明固定为“数据来源于TSP车联网平台”，不展示在线车辆覆盖数。
- 首页预警区标题固定展示“最新预警 · 待处置 X 条”；支持 SOC 过低（橙）、离线超时（黄）、车辆故障/故障报警（红）三类预警，默认先按预警等级、再按故障、离线超时、SOC 过低的类型优先级排序。
- 每条预警需展示预警等级、触发至今的持续时长与车辆状态；SOC≤15%、车辆故障、离线超时≥48 小时为“紧急”，其余为“一般”。车辆状态按充电中、行驶中、静止归类。
- 车辆信息窗底部保留“单车监控”“历史轨迹”，并新增用户明确要求的“任务单”“打电话”操作；任务单进入当前车辆的任务列表，打电话使用 H5 `tel:` 链接。
- 首页融合既有业务看板和车队监管：不得丢弃原首页的 5 个趟次指标、15 个业务入口、“昨日发货量（t）”与“任务单及时送达率（%）”两块报表；在此基础上加入实时车辆在线、SOC 预警和监控入口，按“经营 → 监管 → 业务 → 报表 → 预警”组织。
- 车队长小程序全量页面需按高质量产品标准重构，而非只换色或覆盖样式：地图、首页、列表、详情、轨迹、日志、预警、报表、下载任务及“我的”均须属于同一套移动端调度设计系统。
- 不得用空白方块、文字符号或粗糙 CSS 图形替代缺失素材和图标。优先复用项目已安装的真实图标库；确无可用资源时，先检索可用资产或生成与视觉体系一致的素材。所有页面必须保留并打磨高频交互、明确的状态反馈、合理的转场动画与降低动态效果偏好支持。
- 组织范围仅在首页的居中“页面标题 + 当前组织范围”顶部组件中切换；监控、我的仅保留同款居中页面标题。监控地图的筛选抽屉与定位控件从标题下方开始。首页“经营调度”首张待办分析卡不得采用旧主题上拉覆盖效果；调度工作台入口中的图标与文字须在同一垂直中心轴上对齐。
- 监控地图的场站列表与定位控件应紧邻纵向排布：无信息卡时位于底部栏上方；车辆信息窗或场站卡显示时，联动移动到卡片右上方。工具栏必须在完成位置测量后再显示，并始终位于信息卡下层、与卡片留出安全间距，不得遮挡卡片内容。
- 车队长端场站列表的页面内容必须从固定顶部导航下方开始独立滚动；搜索框固定放在距离筛选上方，搜索与“不限 / 10km / 20km / 50km”标签均不得被顶部标题遮挡。
- 监控页采用独立的全宽搜索栏与状态统计标签：关键词搜索（车牌、挂车牌、当前司机、VIN）可和状态标签组合筛选。监控车辆信息卡与车辆列表卡均展示挂车车牌和当前司机，方便车头—挂车—司机关系的快速确认。
- 监控地图筛选常驻平铺在搜索框下方，仅展示“全部 / 在线 / 离线 / 有任务 / 待运输 / 无任务”六个标签；不使用二次展开。六个标签互斥单选，每次只按一个标签过滤地图、聚合点和车牌清单；“全部”恢复默认视图。
- 挂车与当前司机不是弱化的辅助文字：在监控车辆信息卡和车辆列表卡中均使用两个独立的信息单元，字段名与主值分层显示，卡片高度须为该信息留出充足的阅读空间。
- 监控车辆信息卡在展示挂车与司机后，默认高度必须完整容纳底部四个快捷操作（单车监控、历史轨迹、任务单、打电话）及其 44px 以上触控区，不允许裁切遮挡。
- 监控车辆信息窗默认直接展示完整内容和四个快捷操作；顶部手柄仅支持向下收起信息窗，不再提供“点击展开”状态。信息窗收起后，点击任意地图车辆会再次弹入。
- 历史轨迹页时间筛选与行车日志采用同一套“今天 / 近 3 天 / 近 7 天 / 自定义”口径；自定义支持跨天起止时间。时间栏下固定展示该范围的行程段数、总里程和总行驶时长；从单条行车日志查看轨迹时，进入历史轨迹并自动选中“自定义”及对应行程段。
- 历史轨迹页不在地图上叠放行程时段列表；时段改为底部回放控制上方的横向滑动卡片，展示时间、里程、SOC 消耗与起止地点简称。非选中行程轨迹使用半透明灰色，当前行程按低速黄 / 高速绿分段高亮，并保留“起 / 终”标记与回放车辆联动。
- 历史轨迹回放控制按“完整起终地址 → 进度 → 操作”组织；进度信息统一使用 HH:mm。倍速固定为 1x / 2x / 4x，默认 1x 并记忆上次选择；单段回放结束后停在终点。
- 历史轨迹页不展示联动数据、参数 Tab、报警红点或回放报警弹窗，专注地图、行程卡与回放控制。
- 监控车辆信息窗高度按可见内容自适应，完整显示四个快捷操作后立即收束；不得用过大的固定高度制造无意义留白。
- 监控页点击“全部 / 在线 / 离线 / 从未上线”状态标签后，除筛选地图外还要在标签下方展开对应车牌清单；清单可按当前关键词继续收窄，选中车辆会定位并弹出信息窗，底部必须提供“收起车辆列表”操作。
- 状态标签同时是车牌清单开关：清单打开时再次点击当前激活标签应直接关闭清单；点击其他状态标签则切换筛选并打开对应清单。
- 监控地图按距离采用圆点计数聚合：相邻点以 56px 网格合成 44px 数字圆点，点击后平滑放大一级半；单车继续使用车辆图标。仅当前选中车辆始终独立显示；异常车辆随地图缩小参与聚合，聚合圆点以在线绿、离线灰、混合蓝、含异常橙表达状态和风险。
- 监控地图右侧固定采用“图层开关 → 场站列表 → 当前位置”顺序。图层按钮展开临时浮层，分别控制车辆点位（含聚合点）与充电场站点位，默认均开启；全开为蓝色、仅一层为浅蓝并显示“1”、全关为灰色。关闭车辆层须同时关闭车辆信息卡和车牌清单；关闭场站层须关闭场站卡。浮层点击地图空白处自动收起，筛选条件不随图层开关清空；从车牌清单选择车辆或进入场站列表时，分别自动恢复对应图层。
- 安全告警地图入口（右侧告警图标与数量角标）调整为下一期迭代内容，当前监控地图暂不展示；已实现的告警事件口径、独立列表页与车辆定位联动代码保留，后续恢复入口即可启用。恢复后：角标统计全部仍在发生中的告警事件（同车多事件分别计数），紧急风险为红色、仅一般风险为橙色，数字最多显示 99+；首期展示 SOC 过低、行驶中停留、装卸货电子围栏内停留，已知悉不代表风险恢复。
- 同一个电子围栏可以同时关联开始片区和结束片区，不建立全局 1:1 归属约束；片区类型决定该关联承担的趟次起点或终点角色，更新某一片区地址池时不得清除围栏在另一类型片区中的既有关联。
- 趟次统计报表“查看明细”页不展示导出入口；趟次总耗时统一换算并仅以 `h` 展示。运输任务明细统一展示任务开始时间与任务结束时间，装货空驶单同样必须有完整起止时间。
- 趟次统计报表的行驶时效后固定展示“充电等待时效”；查看明细的运输任务明细同样在行驶时效后展示该字段。计算口径固定为“充电开始时间 − 进入充电站电子围栏时间”，仅在两个事件时间齐全且顺序有效时展示，未采集时统一显示“—”。
- 车辆状态采用两级互斥口径：一级为全量车辆的“在线 / 离线 / 从未上线”；二级仅在线车辆有效，按“充电状态=充电中”优先，其次“行驶状态=行驶中或车速>0”为“行驶中”，其余为“驻车静止”。车辆卡片中车牌号后方展示二级状态；离线、从未上线不展示二级状态。
- 车辆列表卡片与监控地图车辆信息窗的首行信息顺序固定为“车牌 → 二级运行状态（仅在线）→ TMS 任务状态（有任务 / 待运输 / 无任务 / 停运）→ 一级联网状态”。
- 单车监控页首屏第一层为紧凑状态概览栏，展示“车牌 → 主状态（优先二级运行状态，非在线时显示一级状态）→ 最后更新时间”；车型仅作为弱化辅助信息，不使用深蓝色大尺寸头部栏。
- 单车监控页第二层为 2×2 核心运营指标矩阵：剩余电量（SOC + 进度条）、当前车速、今日行驶里程（按当日行车日志聚合）和累计总里程；完整实时字段保留在下方 Tab，不重复充当概览。
- 单车监控页第三层在核心指标后、明细 Tab 前提供横向高频入口：历史轨迹、行车日志、充电日志、单车日报；均跳转既有 TSP 页面，不新增业务功能。
- 单车监控页第四层为下滑可见的可选 TMS 业务信息区，展示挂车车牌、所属司机、任务状态；TMS 标记不可用、异常或全部字段为空时整区隐藏，不影响 TSP 核心监控。
- 单车监控页第五层为下滑查阅的“明细数据 / 深度排查”Tab 区，固定排序为：整车数据 → 电池储能 → 驱动电机 → 报警数据 → 定位数据 → 极值数据；纯电车辆不展示发动机数据。各 Tab 状态、告警与关键业务字段优先于纯技术参数。
- 历史轨迹页页头仅展示“历史轨迹”标题；切换行程时地图自动适配当前选中时段，不提供“轨迹全览”操作。
- 车辆任务单页固定顶部“返回 + 任务单”，导航下方使用粘性车辆信息锚定栏：车牌、统一 TMS 任务状态标签、司机与挂车；无绑定时分别显示“暂无司机”“未绑定挂车”。
- 车辆任务单的页面内视图固定为“进行中任务 / 历史任务”：前者仅展示未结束任务，后者展示已完成、已关闭任务；有未结束任务时默认前者，否则默认历史任务。车辆信息锚定栏的 TMS 胶囊仅表达全局业务状态，不参与该视图切换。
- 任务单“进行中任务”最多展示 1 张任务卡，不采用列表；字段固定为任务单号、局部任务状态、要求发车时间、最新定位时间和可展开查看完整地址的最新位置。无有效进行中任务时展示空状态；时间统一为 `YYYY-MM-DD HH:mm:ss`。
- 任务单可复用司机端的运输节点信息，但车队长端只提供查看与追踪：进行中卡固定展示当前运输节点及装货地、卸货地摘要；历史任务卡以“起 / 终”节点展示任务路线和完成/关闭状态，不提供司机端的到达、导航、上报位置等执行操作。
- 任务单页以现有司机端小程序截图为视觉基准进行 1:1 复刻：执行中页使用“任务信息 / 路线信息”蓝色双栏、平铺任务字段、历史上报事件、运输节点及蓝色主操作外观；历史页使用“已完成任务”标题、时间/单号查询、全部/已驳回状态栏和起终点任务块。车队长端的同名上报/到达按钮只用于查看定位信息，不触发司机端任务执行。
- 任务单列表页（已完成任务的查询与连续任务块）当前暂不在车队长端展示；只保留执行中任务的单据详情。历史任务演示数据继续保留，后续恢复列表无需重建数据层。
- 监控车辆信息窗保留“任务单”快捷按钮的图标与视觉位置，但当前不绑定点击跳转或其他交互；单车监控、历史轨迹、打电话继续可用。
- 单车监控入口的行车日志页不在页头展示车牌副标题；筛选顺序固定为“自定义时间（起止日期）→ 今日 / 近 7 天 / 近 30 天 Tab”。自定义时间不再作为 Tab，选择起止日期后直接按该范围筛选并显示“自定义”口径。
- 单车监控入口的充电日志页与行车日志采用相同筛选层级和无车牌页头；单车充电记录不提供“位置查看”或整卡位置跳转。车队级今日充电日志继续保留其位置查看能力。
- 单车监控页当前暂不提供“单车日报”快捷入口或跳转；日报页面实现与数据保留，待功能恢复时再重新接入入口。
- 单车监控的 TMS 业务信息（挂车车牌、所属司机、任务状态）融合到顶部车辆状态卡的底部，不再单独占用一张业务信息卡；TMS 不可用或异常时该内联信息仍自动隐藏。
- 单车监控顶部状态卡不展示车型或车辆品牌，仅保留车牌、运行状态、最后更新时间与内联 TMS 业务信息。
- 顶部状态卡的内联 TMS 第三项：车辆任务状态为“有任务”或“待运输”时，标签显示“任务单号”并直接展示当前任务编号，不重复展示这两个状态文字；无任务、停运等其他状态继续展示“任务状态”。
- 监控车辆信息卡移除“详情”入口，车辆详情页不再作为可路由页面；卡片底部隐藏“单车监控 / 任务单 / 历史轨迹”三个快捷按钮，仅保留“打电话”。车辆列表与预警原有的详情跳转统一回到监控地图并定位对应车辆。
- 监控车辆信息卡右上角固定展示紧凑天气组件（天气图标、温度、天气状况）；四项指标中的一级在线状态替换为“车重 x t”，累计总里程替换为按当日行车日志聚合的“今日里程”。
- 监控车辆信息卡的位置与更新时间固定为同一行：位置在左且超长省略，更新时间右对齐，不再单独占用第二行。
